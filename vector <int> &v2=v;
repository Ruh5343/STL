#include <bits/stdc++.h>
using namespace std;
void print(vector <int> &v){
for (int i=0; i<v.size(); i++ ){
    cout<<v[i]<<" ";
}
v.push_back(45); //If you want changes inside the function to affect the original vector, pass by reference: void print(vector <int> &v)
cout<<endl;
}
int main(){
vector <int> v(5, 3);//10 is the size. and 3 is the number entered previous
cout<<"before entering extra value. the size= "<<v.size()<<endl;
v.pop_back();
print(v);
v.push_back(7);

cout<<"After entering extra value. the size= "<<v.size()<<endl;

print(v);
vector <int> &v2=v; /*No new vector, No copy, Just another name for the same thing*/
v2.push_back(111);
cout<<"V2 = ";
print(v2);
return 0;
}
